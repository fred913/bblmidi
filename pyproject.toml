[project]
name = "bblmidi"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = ["pretty-midi>=0.2.11", "typer>=0.24.1"]

[tool.pyright]
venvPath = "."
venv = ".venv"

[tool.ruff.lint]
select = ["I"]
